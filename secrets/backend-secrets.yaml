apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: shop
type: Opaque
stringData:
  # Database URL nếu bạn dùng dạng full URI, ví dụ:
  # postgresql://<user>:<password>@<host>:<port>/<dbname>
  DATABASE_URL: "postgresql://shopuser:changeme-dev@postgres:5432/shopdb"

  # JWT configuration
  JWT_SECRET: "super-secret-key"            # 🔒 thay bằng key mạnh thật
  JWT_EXPIRE_MINUTES: "60"                  # hoặc giá trị bạn muốn

  # MinIO connection
  MINIO_ENDPOINT: "http://minio.shop.svc.cluster.local:9000"
  MINIO_ACCESS_KEY: "minioadmin"            # hoặc giá trị thật từ minio-secrets
  MINIO_SECRET_KEY: "miniopass"            # hoặc giá trị thật từ minio-secrets
  MINIO_BUCKET: "shop-bucket"
  MINIO_SECURE: "false"
  MINIO_PUBLIC_URL: "http://minio.shop.svc.cluster.local:9000/minio-public"
